using System;

namespace Data_Types
{
    class Program
    {
        static void Main(string[] args)
        {
            string phrase = "Giraffe Academy";
            char grade = 'A';
            int age = 30;
            double gpa = 3.3; // 3 data types to represent decimels (float, doublde, decimal)
            Boolean isMale = true;

            Console.WriteLine(3.3);

            Console.ReadLine();
        }
    }
}
